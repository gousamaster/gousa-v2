"use client";
import{useEffect,useState}from"react";import{Plus,Trash2}from"lucide-react";import{toast}from"sonner";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{Textarea}from"@/components/ui/textarea";import{agregarEstudioAnterior,agregarTrabajoAnterior,eliminarHistorialFormativo,obtenerHistorialFormativo,type EstudioAnteriorItem,type TrabajoAnteriorItem}from"@/lib/actions/clientes/historial-formativo-actions";
const fecha=(v:unknown)=>v?new Date(v as string|Date).toLocaleDateString("es-BO"):"Sin fecha";
export function ClienteHistorialFormativoTab({clienteId}:{clienteId:string}){const[trabajos,setTrabajos]=useState<TrabajoAnteriorItem[]>([]),[estudios,setEstudios]=useState<EstudioAnteriorItem[]>([]),[loading,setLoading]=useState(true),[showTrabajo,setShowTrabajo]=useState(false),[showEstudio,setShowEstudio]=useState(false);const[tw,setTw]=useState({lugar:"",fechaIngreso:"",fechaSalida:"",cargo:"",descripcion:""});const[es,setEs]=useState({lugar:"",direccion:"",carrera:"",fechaIngreso:"",fechaFinalizacion:""});async function cargar(){setLoading(true);const r=await obtenerHistorialFormativo(clienteId);if(r.success&&r.data){setTrabajos(r.data.trabajos);setEstudios(r.data.estudios)}setLoading(false)}useEffect(()=>{void cargar()},[clienteId]);async function guardarTrabajo(){if(!tw.lugar.trim())return toast.error("Indica el lugar de trabajo");const r=await agregarTrabajoAnterior(clienteId,{...tw,fechaIngreso:tw.fechaIngreso||null,fechaSalida:tw.fechaSalida||null});if(!r.success)return toast.error(r.error);toast.success("Trabajo anterior agregado");setTw({lugar:"",fechaIngreso:"",fechaSalida:"",cargo:"",descripcion:""});setShowTrabajo(false);await cargar()}async function guardarEstudio(){if(!es.lugar.trim())return toast.error("Indica el lugar de estudio");const r=await agregarEstudioAnterior(clienteId,{...es,fechaIngreso:es.fechaIngreso||null,fechaFinalizacion:es.fechaFinalizacion||null});if(!r.success)return toast.error(r.error);toast.success("Estudio anterior agregado");setEs({lugar:"",direccion:"",carrera:"",fechaIngreso:"",fechaFinalizacion:""});setShowEstudio(false);await cargar()}async function eliminar(tipo:"TRABAJO"|"ESTUDIO",id:string){const r=await eliminarHistorialFormativo(clienteId,tipo,id);if(!r.success)return toast.error(r.error);toast.success("Antecedente eliminado");await cargar()}return <div className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><div className="flex items-center justify-between gap-3"><div><CardTitle>Trabajos anteriores</CardTitle><p className="mt-1 text-sm text-muted-foreground">Historial laboral previo del cliente.</p></div><Button size="sm" onClick={()=>setShowTrabajo(v=>!v)}><Plus className="mr-2 h-4 w-4"/>Agregar</Button></div></CardHeader><CardContent className="space-y-4">{showTrabajo&&<div className="rounded-lg border p-4 space-y-3"><div><Label>Nombre del lugar / empresa *</Label><Input value={tw.lugar} onChange={e=>setTw({...tw,lugar:e.target.value})}/></div><div><Label>Cargo</Label><Input value={tw.cargo} onChange={e=>setTw({...tw,cargo:e.target.value})}/></div><div className="grid grid-cols-2 gap-3"><div><Label>Fecha ingreso</Label><Input type="date" value={tw.fechaIngreso} onChange={e=>setTw({...tw,fechaIngreso:e.target.value})}/></div><div><Label>Fecha salida</Label><Input type="date" value={tw.fechaSalida} onChange={e=>setTw({...tw,fechaSalida:e.target.value})}/></div></div><div><Label>Descripción</Label><Textarea rows={3} value={tw.descripcion} onChange={e=>setTw({...tw,descripcion:e.target.value})}/></div><Button size="sm" onClick={()=>void guardarTrabajo()}>Guardar trabajo</Button></div>}{loading?<p className="text-sm text-muted-foreground">Cargando...</p>:trabajos.length===0?<p className="text-sm text-muted-foreground">Sin trabajos anteriores registrados.</p>:trabajos.map(t=><div key={t.id} className="rounded-lg border p-3 text-sm"><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{t.lugar}</p><p>{t.cargo||"Cargo no registrado"}</p><p className="text-xs text-muted-foreground">{fecha(t.fechaIngreso)} → {fecha(t.fechaSalida)}</p>{t.descripcion&&<p className="mt-2 text-muted-foreground">{t.descripcion}</p>}</div><Button variant="ghost" size="sm" onClick={()=>void eliminar("TRABAJO",t.id)}><Trash2 className="h-4 w-4"/></Button></div></div>)}</CardContent></Card><Card><CardHeader><div className="flex items-center justify-between gap-3"><div><CardTitle>Estudios anteriores</CardTitle><p className="mt-1 text-sm text-muted-foreground">Instituciones y formación previa.</p></div><Button size="sm" onClick={()=>setShowEstudio(v=>!v)}><Plus className="mr-2 h-4 w-4"/>Agregar</Button></div></CardHeader><CardContent className="space-y-4">{showEstudio&&<div className="rounded-lg border p-4 space-y-3"><div><Label>Lugar / institución *</Label><Input value={es.lugar} onChange={e=>setEs({...es,lugar:e.target.value})}/></div><div><Label>Dirección</Label><Input value={es.direccion} onChange={e=>setEs({...es,direccion:e.target.value})}/></div><div><Label>Carrera / estudio</Label><Input value={es.carrera} onChange={e=>setEs({...es,carrera:e.target.value})}/></div><div className="grid grid-cols-2 gap-3"><div><Label>Ingreso</Label><Input type="date" value={es.fechaIngreso} onChange={e=>setEs({...es,fechaIngreso:e.target.value})}/></div><div><Label>Finalización</Label><Input type="date" value={es.fechaFinalizacion} onChange={e=>setEs({...es,fechaFinalizacion:e.target.value})}/></div></div><Button size="sm" onClick={()=>void guardarEstudio()}>Guardar estudio</Button></div>}{loading?<p className="text-sm text-muted-foreground">Cargando...</p>:estudios.length===0?<p className="text-sm text-muted-foreground">Sin estudios anteriores registrados.</p>:estudios.map(e=><div key={e.id} className="rounded-lg border p-3 text-sm"><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{e.lugar}</p><p>{e.carrera||"Carrera/estudio no registrado"}</p>{e.direccion&&<p className="text-muted-foreground">{e.direccion}</p>}<p className="text-xs text-muted-foreground">{fecha(e.fechaIngreso)} → {fecha(e.fechaFinalizacion)}</p></div><Button variant="ghost" size="sm" onClick={()=>void eliminar("ESTUDIO",e.id)}><Trash2 className="h-4 w-4"/></Button></div></div>)}</CardContent></Card></div>}
