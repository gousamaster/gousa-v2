"use client";
import{useEffect,useState}from"react";import{Plus,Trash2}from"lucide-react";import{toast}from"sonner";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{Textarea}from"@/components/ui/textarea";import{agregarEstudioAnterior,agregarTrabajoAnterior,eliminarHistorialFormativo,obtenerHistorialFormativo,type EstudioAnteriorItem,type TrabajoAnteriorItem}from"@/lib/actions/clientes/historial-formativo-actions";
const fecha=(v:unknown)=>v?new Date(v as string|Date).toLocaleDateString("es-BO"):"Sin fecha";

export function TrabajosAnterioresAdicionales({clienteId}:{clienteId:string}){const[items,setItems]=useState<TrabajoAnteriorItem[]>([]),[loading,setLoading]=useState(true),[show,setShow]=useState(false);const[form,setForm]=useState({lugar:"",fechaIngreso:"",fechaSalida:"",cargo:"",descripcion:""});async function cargar(){setLoading(true);const r=await obtenerHistorialFormativo(clienteId);if(r.success&&r.data)setItems(r.data.trabajos);setLoading(false)}useEffect(()=>{void cargar()},[clienteId]);async function guardar(){if(!form.lugar.trim())return toast.error("Indica el lugar de trabajo");const r=await agregarTrabajoAnterior(clienteId,{...form,fechaIngreso:form.fechaIngreso||null,fechaSalida:form.fechaSalida||null});if(!r.success)return toast.error(r.error);toast.success("Trabajo anterior agregado");setForm({lugar:"",fechaIngreso:"",fechaSalida:"",cargo:"",descripcion:""});setShow(false);await cargar()}async function eliminar(id:string){const r=await eliminarHistorialFormativo(clienteId,"TRABAJO",id);if(!r.success)return toast.error(r.error);toast.success("Trabajo eliminado");await cargar()}return <section className="space-y-4 border-t pt-6"><div className="flex items-center justify-between gap-3"><div><h3 className="text-sm font-semibold">Otros trabajos anteriores</h3><p className="text-xs text-muted-foreground">Agrega tantos antecedentes laborales como sea necesario.</p></div><Button type="button" size="sm" variant="outline" onClick={()=>setShow(v=>!v)}><Plus className="mr-2 h-4 w-4"/>Añadir un trabajo anterior</Button></div>{show&&<div className="rounded-lg border p-4 space-y-3"><div className="grid grid-cols-2 gap-3"><div><Label>Empresa / lugar *</Label><Input value={form.lugar} onChange={e=>setForm({...form,lugar:e.target.value})}/></div><div><Label>Cargo</Label><Input value={form.cargo} onChange={e=>setForm({...form,cargo:e.target.value})}/></div></div><div><Label>Descripción / funciones</Label><Textarea rows={3} value={form.descripcion} onChange={e=>setForm({...form,descripcion:e.target.value})}/></div><div className="grid grid-cols-2 gap-3"><div><Label>Fecha ingreso</Label><Input type="date" value={form.fechaIngreso} onChange={e=>setForm({...form,fechaIngreso:e.target.value})}/></div><div><Label>Fecha salida</Label><Input type="date" value={form.fechaSalida} onChange={e=>setForm({...form,fechaSalida:e.target.value})}/></div></div><Button type="button" size="sm" onClick={()=>void guardar()}>Guardar trabajo anterior</Button></div>}{loading?<p className="text-sm text-muted-foreground">Cargando antecedentes...</p>:items.length>0&&<div className="space-y-2">{items.map((t,i)=><div key={t.id} className="rounded-lg border p-3 text-sm"><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{i+1}. {t.lugar}</p><p>{t.cargo||"Cargo no registrado"}</p><p className="text-xs text-muted-foreground">{fecha(t.fechaIngreso)} → {fecha(t.fechaSalida)}</p>{t.descripcion&&<p className="mt-2 text-muted-foreground">{t.descripcion}</p>}</div><Button type="button" variant="ghost" size="sm" onClick={()=>void eliminar(t.id)}><Trash2 className="h-4 w-4"/></Button></div></div>)}</div>}</section>}

export function EstudiosAnterioresAdicionales({clienteId}:{clienteId:string}){const[items,setItems]=useState<EstudioAnteriorItem[]>([]),[loading,setLoading]=useState(true),[show,setShow]=useState(false);const[form,setForm]=useState({lugar:"",direccion:"",carrera:"",fechaIngreso:"",fechaFinalizacion:""});async function cargar(){setLoading(true);const r=await obtenerHistorialFormativo(clienteId);if(r.success&&r.data)setItems(r.data.estudios);setLoading(false)}useEffect(()=>{void cargar()},[clienteId]);async function guardar(){if(!form.lugar.trim())return toast.error("Indica el lugar de estudio");const r=await agregarEstudioAnterior(clienteId,{...form,fechaIngreso:form.fechaIngreso||null,fechaFinalizacion:form.fechaFinalizacion||null});if(!r.success)return toast.error(r.error);toast.success("Estudio anterior agregado");setForm({lugar:"",direccion:"",carrera:"",fechaIngreso:"",fechaFinalizacion:""});setShow(false);await cargar()}async function eliminar(id:string){const r=await eliminarHistorialFormativo(clienteId,"ESTUDIO",id);if(!r.success)return toast.error(r.error);toast.success("Estudio eliminado");await cargar()}return <section className="space-y-4 border-t pt-6"><div className="flex items-center justify-between gap-3"><div><h3 className="text-sm font-semibold">Otros estudios anteriores</h3><p className="text-xs text-muted-foreground">Agrega especialidades, carreras o instituciones adicionales.</p></div><Button type="button" size="sm" variant="outline" onClick={()=>setShow(v=>!v)}><Plus className="mr-2 h-4 w-4"/>Añadir un estudio anterior</Button></div>{show&&<div className="rounded-lg border p-4 space-y-3"><div className="grid grid-cols-2 gap-3"><div><Label>Institución *</Label><Input value={form.lugar} onChange={e=>setForm({...form,lugar:e.target.value})}/></div><div><Label>Carrera / programa</Label><Input value={form.carrera} onChange={e=>setForm({...form,carrera:e.target.value})}/></div></div><div><Label>Dirección</Label><Input value={form.direccion} onChange={e=>setForm({...form,direccion:e.target.value})}/></div><div className="grid grid-cols-2 gap-3"><div><Label>Fecha inicio</Label><Input type="date" value={form.fechaIngreso} onChange={e=>setForm({...form,fechaIngreso:e.target.value})}/></div><div><Label>Fecha finalización</Label><Input type="date" value={form.fechaFinalizacion} onChange={e=>setForm({...form,fechaFinalizacion:e.target.value})}/></div></div><Button type="button" size="sm" onClick={()=>void guardar()}>Guardar estudio anterior</Button></div>}{loading?<p className="text-sm text-muted-foreground">Cargando antecedentes...</p>:items.length>0&&<div className="space-y-2">{items.map((e,i)=><div key={e.id} className="rounded-lg border p-3 text-sm"><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{i+1}. {e.lugar}</p><p>{e.carrera||"Carrera/estudio no registrado"}</p>{e.direccion&&<p className="text-muted-foreground">{e.direccion}</p>}<p className="text-xs text-muted-foreground">{fecha(e.fechaIngreso)} → {fecha(e.fechaFinalizacion)}</p></div><Button type="button" variant="ghost" size="sm" onClick={()=>void eliminar(e.id)}><Trash2 className="h-4 w-4"/></Button></div></div>)}</div>}</section>}

export function ClienteHistorialFormativoTab({clienteId}:{clienteId:string}){return <div className="space-y-8"><TrabajosAnterioresAdicionales clienteId={clienteId}/><EstudiosAnterioresAdicionales clienteId={clienteId}/></div>}
