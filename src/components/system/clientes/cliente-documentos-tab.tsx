"use client";
import {useEffect,useMemo,useState} from "react";import{CheckCircle2,Eye,ExternalLink,FileUp,RefreshCcw,X}from"lucide-react";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";
const TIPOS=[["PASAPORTE","Pasaporte"],["CEDULA_IDENTIDAD","Cédula de identidad"],["CONFIRMACION_CITA","Confirmación de cita"],["FORMULARIO_LLENADO","Formulario llenado"],["CONFIRMACION_FORMULARIO","Confirmación de formulario"]]as const;type Documento={id:string;tipo:string;nombreArchivo:string;mimeType:string;tamanoBytes:number;updatedAt:string;subidoPorNombre:string};type Preview={documento:Documento;label:string;url:string};function tamano(bytes:number){if(bytes<1024)return`${bytes} B`;if(bytes<1024*1024)return`${(bytes/1024).toFixed(0)} KB`;return`${(bytes/1024/1024).toFixed(1)} MB`}async function leerRespuesta(r:Response){const text=await r.text();if(!text)return{}as{error?:string;documentos?:Documento[]};try{return JSON.parse(text)as{error?:string;documentos?:Documento[]}}catch{return{error:r.ok?"Respuesta inesperada del servidor":"El servidor no pudo procesar la solicitud"}}}
export function ClienteDocumentosTab({clienteId}:{clienteId:string}){const[documentos,setDocumentos]=useState<Documento[]>([]),[loading,setLoading]=useState(true),[uploading,setUploading]=useState<string|null>(null),[error,setError]=useState<string|null>(null),[success,setSuccess]=useState<string|null>(null),[preview,setPreview]=useState<Preview|null>(null);async function cargar(){setLoading(true);setError(null);try{const r=await fetch(`/api/clientes/${clienteId}/documentos`,{cache:"no-store"});const j=await leerRespuesta(r);if(!r.ok)throw new Error(j.error??"No se pudieron cargar los documentos");setDocumentos(j.documentos??[])}catch(e){setError(e instanceof Error?e.message:"No se pudieron cargar los documentos")}finally{setLoading(false)}}useEffect(()=>{void cargar()},[clienteId]);useEffect(()=>{if(!preview)return;const cerrar=(e:KeyboardEvent)=>{if(e.key==="Escape")setPreview(null)};window.addEventListener("keydown",cerrar);return()=>window.removeEventListener("keydown",cerrar)},[preview]);const mapa=useMemo(()=>new Map(documentos.map(d=>[d.tipo,d])),[documentos]);async function subir(tipo:string,file:File|null){if(!file)return;setUploading(tipo);setError(null);setSuccess(null);try{const fd=new FormData();fd.append("tipo",tipo);fd.append("archivo",file);const r=await fetch(`/api/clientes/${clienteId}/documentos`,{method:"POST",body:fd});const j=await leerRespuesta(r);if(!r.ok)throw new Error(j.error??"No se pudo guardar el documento");setSuccess("Documento guardado correctamente");await cargar()}catch(e){setError(e instanceof Error?e.message:"No se pudo guardar el documento")}finally{setUploading(null)}}function abrirPreview(d:Documento,label:string){setPreview({documento:d,label,url:`/api/clientes/${clienteId}/documentos/${d.tipo}`})}return <><div className="space-y-4"><div className="flex items-center justify-between"><div><h3 className="text-lg font-semibold">Documentos esenciales</h3><p className="text-sm text-muted-foreground">PDF, JPG o PNG · máximo 3 MB.</p></div><Button variant="outline" size="sm" onClick={()=>void cargar()} disabled={loading}><RefreshCcw className="mr-2 h-4 w-4"/>Actualizar</Button></div>{error&&<div className="rounded-md border p-3 text-sm text-destructive">{error}</div>}{success&&<div className="rounded-md border p-3 text-sm">{success}</div>}<div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{TIPOS.map(([tipo,label])=>{const d=mapa.get(tipo);return <Card key={tipo}><CardHeader className="pb-2"><CardTitle className="flex items-center justify-between text-base"><span>{label}</span>{d?<span className="inline-flex items-center gap-1 text-xs"><CheckCircle2 className="h-3.5 w-3.5"/>Cargado</span>:<span className="text-xs text-muted-foreground">Pendiente</span>}</CardTitle></CardHeader><CardContent className="space-y-3">{d&&<div className="text-xs text-muted-foreground"><div className="truncate font-medium text-foreground">{d.nombreArchivo}</div><div>{tamano(d.tamanoBytes)} · {new Date(d.updatedAt).toLocaleString("es-BO")}</div><div>Subido por: {d.subidoPorNombre}</div></div>}<div className="flex gap-2">{d&&<Button size="sm" variant="outline" onClick={()=>abrirPreview(d,label)}><Eye className="mr-2 h-4 w-4"/>Ver</Button>}<label><input type="file" className="hidden" accept="application/pdf,image/jpeg,image/png" disabled={uploading===tipo} onChange={e=>void subir(tipo,e.target.files?.[0]??null)}/><span className="inline-flex h-9 cursor-pointer items-center rounded-md bg-primary px-3 text-sm font-medium text-primary-foreground"><FileUp className="mr-2 h-4 w-4"/>{uploading===tipo?"Subiendo...":d?"Reemplazar":"Subir"}</span></label></div></CardContent></Card>})}</div></div>{preview&&<div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/65 p-4" onMouseDown={e=>{if(e.currentTarget===e.target)setPreview(null)}}><div className="flex h-[92vh] w-full max-w-6xl flex-col overflow-hidden rounded-xl bg-background"><div className="flex items-center justify-between border-b px-4 py-3"><div><div className="font-semibold">{preview.label}</div><div className="text-xs text-muted-foreground">{preview.documento.nombreArchivo}</div></div><div className="flex gap-2"><Button asChild size="sm" variant="outline"><a href={preview.url} target="_blank" rel="noreferrer"><ExternalLink className="mr-2 h-4 w-4"/>Abrir aparte</a></Button><Button size="icon" variant="ghost" onClick={()=>setPreview(null)}><X className="h-5 w-5"/></Button></div></div><div className="min-h-0 flex-1 p-3">{preview.documento.mimeType==="application/pdf"?<iframe src={preview.url} title={preview.label} className="h-full w-full"/>:<div className="flex h-full items-center justify-center"><img src={preview.url} alt={preview.label} className="max-h-full max-w-full object-contain"/></div>}</div></div></div>}</>}
