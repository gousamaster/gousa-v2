export async function getNexusData(clienteId: string) {
  // Server-side helper — currently unused by API (placeholder for future refactor)
  // We intentionally keep this minimal and read-only.
  return { clienteId };
}
